### Deployment

From Linux/MAC OS

```bash
GIT_USER=vwumumu USE_SSH=true yarn deploy
```

From Windows

```ps
$env:GIT_USER='vwumumu';$env:USE_SSH='true';yarn deploy
```

If you are using GitHub pages for hosting, this command is a convenient way to build the website and push to the `gh-pages` branch.
